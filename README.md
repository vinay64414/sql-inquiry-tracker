# sql-inquiry-tracker
This project builds a Customer Inquiry Tracker using SQL, demonstrating database design and CRUD operations. It manages support tickets, enables data retrieval with filters, updates statuses, removes outdated records, and analyzes inquiries using GROUP BY for actionable insights.
